/**
 * 高德地图配置（JS API 2.0）
 *
 * 使用方法：
 * 1. 前往 https://console.amap.com 申请 Web 端（JS API）Key 与安全密钥（securityJsCode）
 * 2. 将下方 key / securityJsCode 替换为实际值
 * 3. 在 http://localhost:5173 的 Key 安全设置中添加该域名白名单
 */
export default {
  /** 高德 Web 端（JS API）Key */
  key: '',
  /** 高德安全密钥（securityJsCode，v2.0 必填） */
  securityJsCode: ''
}
